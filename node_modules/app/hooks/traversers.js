const { traverse } = require('feathers-hooks-common');

module.exports = {
  nuller() {
    return traverse(function (node) {
      if(node === 'null') { this.update(null); }
    }, traversecontext => traversecontext.params.query)
  }
}
